---
id: rel-2026-03-17-map-runtime-dual-provider-foundation
version: v0.0.0
title: "Dual-provider map runtime foundation"
date: 2026-03-17
published_at: 2026-03-17T18:00:00Z
status: draft
notify_in_app: false
in_app_hours: 24
summary: "Adds a shared map runtime foundation so Google and Mapbox can be tested side by side without changing the current production planner behavior."
---

## Changes
- [ ] [Internal] 🗺️ Added a shared map runtime contract, cookie override flow, and admin debugger controls so Google-only and mixed Mapbox-visual presets can be tested with the same resolver across planner, preview, and OG map surfaces.
- [ ] [Internal] 🧪 Moved the admin map runtime controls into a dedicated Maps debugger tab and added a safer Google fallback when Mapbox basemap requests fail at runtime.
- [ ] [Internal] 🧱 Fixed the Mapbox visual basemap mount so shared planner/blog map surfaces keep a full-height canvas instead of collapsing to an empty viewport during mixed-runtime testing.
- [ ] [Internal] 🧭 Restored shared Google overlay behavior above the Mapbox basemap by hiding only the Google base pane in mixed-runtime mode, so planner interactions, markers, and route layers stay visible while Mapbox visuals are enabled, and kept the debugger on the last admin tab after reloads even if auth finishes resolving a moment later.
- [ ] [Internal] 🛰️ Reworked the trip-page mixed map surface so Google’s placeholder background and attribution chrome no longer cover the Mapbox basemap, while Mapbox now owns direct map interaction and keeps its camera synced with Google-backed overlays.
- [ ] [Internal] ✨ Moved the trip page’s visible city markers, route lines, and label overlays onto native Mapbox rendering in mixed mode, added a cleaner Standard-style basemap profile, softened the initial Mapbox camera experience with a globe-to-trip intro and less aggressive resize churn, and kept the Google canvas hidden while Mapbox is warming up so the two map surfaces stop flashing against each other.
- [ ] [Internal] 🎥 Smoothed the trip page’s mixed Mapbox camera handoff by waiting for gesture completion before syncing back to Google, starting the Mapbox canvas directly from a globe view, increasing fit/recenter safe-zone padding, improving city label spacing, and making Standard-style switches use the supported Mapbox config flow instead of brittle runtime layer mutations.
- [ ] [Internal] 🧩 Stabilized trip-page mixed map state by cleaning up the Mapbox satellite basemap, rebuilding native route layers after style reloads, enlarging recenter safe-zones, shifting marker zoom transitions earlier, and preventing stale deferred trip snapshots from overwriting newer map, city, or timeline changes.
- [ ] [Internal] ✈️ Replayed the dual-provider map foundation onto the latest `origin/main` base, made local history snapshots write before navigation so delayed style commits stop snapping back to stale views, restored country context on the cleaner Mapbox basemaps, surfaced the globe intro before Google overlays finish warming, centered trip labels above their pins, and added curved flight paths with a straight ground shadow on the trip page.
- [ ] [Internal] 🎛️ Extracted trip-page map tuning into a shared provider-aware helper so Google and Mapbox can use different fit padding, focus zooms, marker zoom bands, resize thresholds, and intro-camera timing without scattering new magic numbers across the planner surface, and restored the current-main package baseline so branch builds stop failing on missing shared dependencies.
- [ ] [Internal] 🩹 Hardened the trip-page map helpers against malformed coordinate objects so older or partially enriched trips skip invalid city/activity positions instead of crashing while marker groups are being prepared.
- [ ] [Internal] 🧱 Centralized finite-coordinate validation for the trip page so both the map renderer and trip-country enrichment skip malformed latitude/longitude payloads instead of crashing during trip boot or marker grouping.
- [ ] [Internal] 🧼 Added a runtime trip-payload normalizer at the route boundary so malformed legacy item numbers, invalid coordinates, and partial saved view settings are cleaned before the planner renders, preventing older trips from crashing on trip open.
- [ ] [Internal] 🩺 Traced a post-rebase trip-open crash to the planner workspace zoom badge, then restored the missing parent `zoomLevel` wiring and added a finite-number fallback plus regression coverage so the trip page cannot crash before the planner map mounts.
- [ ] [Internal] 🧰 Extracted provider-aware trip-map presentation rules for label offset, future image-badge markers, and curved-flight shadow styling so Google and Mapbox visual tuning can keep diverging without adding more one-off constants inside the trip-page map renderer.
- [ ] [Internal] 🌍 Tuned the trip-page Mapbox renderer with provider-specific floating vs docked fit padding, a flatter Mercator resting projection with globe intro reserved for larger docked loads, white country borders without state borders, major-city-only basemap labels, lighter dark-mode route rendering, and style-matched floating-map surfaces so Mapbox can be polished independently from Google without re-coupling the planner map logic.
- [ ] [Internal] 🧷 Extracted city-marker HTML into a dedicated trip-map presenter with future-ready image-badge support, so richer photo markers can be introduced later without reopening the provider split or expanding the main trip map renderer again.
- [ ] [Internal] 🎥 Reworked the trip-page Mapbox runtime to resolve intro camera decisions from the real viewport size, cancel globe mode when the map becomes floating or too small, refit later when switching back to docked, redraw overlays as soon as a new style is ready, and tighten floating safe zones so small previews stay flat and route-focused.
- [ ] [Internal] 🧭 Extracted provider-aware trip-map intro choreography into a dedicated camera helper so the globe/fly-in sequence can keep diverging between Google and Mapbox without re-expanding the basemap runtime or reintroducing hardcoded animation values there.
- [ ] [Internal] 🧯 Hardened the trip-page Mapbox style-switch path so provider redraws and projection updates wait for the new style to be genuinely ready, preventing intermittent “Style is not done loading” errors during itinerary map transitions.
- [ ] [Internal] 💾 Persisted the debugger open state and last active tab so reloads return to the same workspace, including the Maps tab during provider testing.
- [ ] [Internal] 🧭 Extracted provider-specific location search and route computation into dedicated services so the planner renderer can stay visually stable while map providers are evaluated behind cleaner seams.
- [ ] [Internal] 🖼️ Made trip preview and OG/static map generation runtime-aware, including provider-specific cache separation so Google and Mapbox preview assets do not collide during testing.
- [ ] [Internal] 🔐 Documented the required Mapbox runtime environment keys in the example env file so local and edge testing can be configured without guesswork.
- [ ] [Internal] 🧰 Fixed current-main replay regressions by preserving owner trip map styles on refresh, repairing the Trip Info modal title-edit contract, deferring the first Mapbox camera sync until the surface is ready so the globe intro can actually run, tightening boundary cleanup to keep country outlines while hiding state-level borders, and restoring stronger colored route layers for dark Mapbox styles.
- [ ] [Internal] 🏷️ Extracted trip city-label presentation into a dedicated helper with provider-aware width and theme rules, so centered labels, future photo markers, and later Google-vs-Mapbox label tuning can evolve without reopening the main trip map renderer.
- [ ] [Internal] 🛣️ Made the trip-page Mapbox renderer paint visible fallback routes immediately on first load while realistic directions are still computing, switched the intro fly-in back to a north-up camera, widened route-aware fit bounds with docked-safe padding, and restored satellite country context plus brighter clean-dark route colors through shared provider tuning instead of new one-off page constants.
- [ ] [Internal] 🧭 Tightened trip-page Mapbox polish so only `admin_0`-level country borders survive while state-style boundaries and clean-style road clutter are hidden more aggressively, increased docked safe-space padding around route fits, and ported the latest live-trip update guard from `main` so transport-type edits stay aligned with delayed history commits.
- [ ] [Internal] 🗺️ Merged the latest `origin/main` trip-view protections into this feature branch, reapplied Mapbox cleanup after style idle so `admin_0` country borders can stay while `admin_1+` boundaries and clean-style clutter get stripped more reliably, darkened light-style country borders, and retuned docked fit padding so Mapbox stops over-zooming out after the earlier safe-zone fix.
- [ ] [Internal] 📱 Restored the current-main mobile trip-details behavior after the merge by wiring the trip selection controller back into the two-step drawer flow, so selecting an item on small screens keeps details collapsed until the second tap and closing the drawer clears the mobile selection again.
- [ ] [Internal] 🧠 Corrected the trip-page deferred commit flow so transport and other data edits keep the view snapshot they were scheduled with, and absorb any older pending visual commit instead of firing a second stale history entry that can snap Mapbox controls back after a toast.
